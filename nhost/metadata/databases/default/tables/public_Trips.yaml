table:
  name: trips
  schema: public
array_relationships:
  - name: booking
    using:
      foreign_key_constraint_on:
        column: trip_id
        table:
          name: bookings
          schema: public
  - name: bookings
    using:
      foreign_key_constraint_on:
        column: trip_id
        table:
          name: bookings
          schema: public
  - name: notifications
    using:
      foreign_key_constraint_on:
        column: trip_id
        table:
          name: notifications
          schema: public
