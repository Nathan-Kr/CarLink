table:
  name: trip
  schema: public
array_relationships:
  - name: booking
    using:
      foreign_key_constraint_on:
        column: trip_id
        table:
          name: bookings
          schema: public
  - name: bookings
    using:
      foreign_key_constraint_on:
        column: trip_id
        table:
          name: bookings
          schema: public
